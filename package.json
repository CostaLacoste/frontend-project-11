{
  "name": "@hexlet/code",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run dev --prefix code",
    "build": "npm run build --prefix code",
    "preview": "npm run preview --prefix code"
  },
  "dependencies": {
    "bootstrap": "^5.3.8",
    "i18next": "^26.0.3",
    "yup": "^1.7.1"
  },
  "devDependencies": {
    "vite": "^8.0.1"
  }
}
